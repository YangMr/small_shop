<view class="my-container"> 
  <view class="header">
    <view class="bg"></view>
    <!-- <open-data type="userAvatarUrl" style="width:80rpx; height:80rpx;"></open-data>
    <open-data type="userNickName"></open-data> -->
    <view class="userinfo" wx:if="{{!flag}}">
      <button class="user-button" open-type="getUserInfo" bindgetuserinfo="onGetUserInfo" plain="true">
        <image src="../../assets/image/login.png" class="image" />
      </button>
      <text>未登录</text>
    </view>  
    <view class="userinfo" wx:if="{{flag}}">
      <image src="{{userInfo.avatarUrl}}" class="image" />
      <text>{{userInfo.nickName}}</text>
    </view>
  </view>


  
</view>

